eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://localhost:1234/eureka
      
spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/SarthakSinha03/Microservice-Config
          clone-on-start: true
          
    
server:
  port: 8084
  
  